prelude: |
  $LOAD_PATH.unshift File.join(File.dirname(ENV["BUNDLE_GEMFILE"]), 'test')
  require 'perf_helper'
  subject = Subject.new
  tracer = Raygun::Apm::Tracer.new
  tracer.start_trace
benchmark:
  simple_call_one_arg_traced: subject.simple_call('foobar')
loop_count: 1500000
