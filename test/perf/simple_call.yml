prelude: |
  $LOAD_PATH.unshift File.join(File.dirname(ENV["BUNDLE_GEMFILE"]), 'test')
  require 'perf_helper'
  subject = Subject.new
benchmark:
  simple_call: subject.blacklist1
loop_count: 1500000
