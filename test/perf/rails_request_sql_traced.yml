prelude: |
  $LOAD_PATH.unshift File.join(File.dirname(ENV["BUNDLE_GEMFILE"]), 'test')
  require 'perf_helper'
  session = rails_prelude(tracer_enabled: true)
benchmark:
  rails_request_sql_traced: session.get('/posts')
loop_count: 50
