prelude: |
  $LOAD_PATH.unshift File.join(File.dirname(ENV["BUNDLE_GEMFILE"]), 'test')
  require 'perf_helper'
  subject = Subject.new
benchmark:
  simple_call_one_arg: subject.simple_call('foobar')
loop_count: 1500000
