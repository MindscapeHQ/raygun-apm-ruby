prelude: |
  $LOAD_PATH.unshift File.join(File.dirname(ENV["BUNDLE_GEMFILE"]), 'test')
  require 'perf_helper'
  subject = Subject.new
  tracer = Raygun::Apm::Tracer.new
  tracer.start_trace
  tracer.add_blacklist 'Subject', 'blacklist1'
benchmark:
  simple_call_traced_blacklisted: subject.blacklist1
loop_count: 1500000
